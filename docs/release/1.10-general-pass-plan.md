# Waypointer 1.10 general pass

Started 2026-09-04 from `origin/release/1.10-rc` at
`8e85aea3e9b847369d1c014a476f29b3d5899849`, in the isolated
`codex/1.10-general-pass` branch. The other worktrees and main/beta are preserved.

## Readiness and scope

Read the RC plan, issue dispositions, and verification evidence before changes.
A fresh GitHub query still lists the same 16 open issues (#117-126, #129-134);
their existing code-level dispositions remain the starting point. The prior
1,758-test gate is baseline evidence, not a live Minecraft check.

Fable service availability was verified using the installed Claude Code 2.1.260,
explicit `claude-fable-5-1`, `--effort xhigh`, and plan permissions. The successful
response's model usage identifies the first-party canonical Fable model.
Fable owns a bounded read-only import/export and catalog review. Sol xhigh
reviewers independently inspect renderer and settings/persistence behavior.
Astra validates findings before assigning implementation and reviews patches.

Codec wire formats, compression algorithms, and codec benchmarks belong to the
parallel `perf/codec-route-benchmark` task and are excluded here. Any findings
at that boundary are reported for integration instead of edited here.

The coordinating task subsequently assigned bounded chat classification caching
to this pass and requested integration of the completed codec task. Its two
commits were cherry-picked unchanged after the general fixes were committed;
the final combined candidate received the same two-target gates.

## Prioritized work

1. **P1: preserve data and recover operations.** Trace import commits, folders,
   export preview completion/cancellation, catalog state, settings save/reload,
   and storage lifecycle. Reproduce confirmed failures with behavior tests.
2. **P1/P2: correct rendered output and lifetime.** Inspect paint, retained mesh
   invalidation, preview resources, reload/disconnect and the two target adapters.
   Keep GPU-dependent conclusions pending a live client.
3. **P2: usable controls and meaningful CPU work.** Fix confirmed misleading or
   inaccessible controls, stale UI state, or repeat work in common paths. Measure
   performance changes on a stated workload before making speed claims.
4. **Review each slice.** Independently challenge Fable findings; use Sol xhigh
   adversarial review for Luna implementation. Run focused tests after arranging
   a serial Gradle slot with the codec task. Use Java 25 and two Gradle workers.
5. **Final gate and handoff.** Run both target builds and unchanged quality gates,
   inspect the final diff, commit reviewed work, and document results and remaining
   live checks. Do not publish a release, close issues, or change main/beta.

Implementation slices and their evidence will be recorded in
`1.10-general-pass-verification.md` as findings are validated.
