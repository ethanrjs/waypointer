# Preview and test quality adversarial review

Reviewed the settled working-tree diff in `/Users/ethanrettinger/projects/waypointer-preview-quality` against `1ea7d084ad29bbd67667e251b37eeec482c903b2` on 2026-09-04. Read-only repository review; no Gradle run or repository edits by this reviewer.

## Result

No open actionable correctness, performance, or resource-lifetime regression found in the reviewed changes.

One useful coverage contract was initially removed: the ordered preview passes. The new direct-emitter tests cannot detect a dropped/reordered actual pass. This was raised during review and is now restored as `depthPrecedesSurfacesAndOutlinesInThePassPlan`, with a name that accurately describes its scope.

## Evidence

- Independently compiled the current painter `Raster` against the supplied Java 25 classpath and compared 5,805,100 face/silhouette samples over all 360 whole-degree yaw values against a separate inverse-camera matrix / ray-slab intersection oracle. Zero mismatches, excluding outline pixels and subpixel silhouette tolerance. The temporary harness is `/tmp/waypointer-preview-adversarial/OrbitProbe.java`.
- Inspected the final repository test oracle: exclusions now depend only on independently calculated geometry, and each rotation requires minimum painted/background sample counts. This avoids an all-outline raster making the primary test vacuous.
- Checked lazy preview texture preparation, same-paint reuse across routes, hiding, deferred retirement, scene replacement, and explicit release. `ExportScreen` advances retirement even when the compact preview is hidden. Closing/releasing resets the prepared scene before reuse. No changed ownership path was found to retain a stale texture reference.
- Checked linear marker numbering against `WaypointGroup` semantics, including orphan subwaypoints, and checked the hover-envelope change against cube versus simplified-billboard geometry.
- The padded-UV correction uses full texel boundaries only for atlases with replicated padding. The existing unpadded world-atlas inset is preserved. The new emitted-UV sampling test probes near both edges of every column and catches the previous compression of border columns.
- The route-paint tests cover global-style-independent static geometry eligibility, retained texture reservation, RGB suppression, and route paint / paint-enabled scene-key invalidation. Their scope is appropriate for a policy/cache regression claim. Existing sequence role-color fallback remains a distinct, intentional rule.
- Reviewed the final UI test rewrites: footer action preservation/order/non-overlap and Done separation, folder control hit boundaries, group control fit/order, and folder-editor containment/control ordering retain useful behavior while reducing dependence on exact coordinates. The duplicate export-flag scenario remains covered by the stronger `ExportScreenTest` case. Setter/getter, method-modifier, constant-copy, and prose-search removals do not remove demonstrated workflow coverage.
- `git diff --check` was clean at review time.

## Limits

This review and its independent harness do not establish live Minecraft GPU upload/binding, interactive resize/hide/reopen behavior, or final on-screen appearance. The parent owns integrated build/test results; those results are not asserted here. No changes to world route-paint precedence were necessary or introduced by this patch.
